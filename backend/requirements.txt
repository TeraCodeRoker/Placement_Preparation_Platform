# Runtime dependencies (pinned) — plain Django backend.
Django==5.1.4
# Pydantic retained as a framework-neutral validation library (§10.2 requires
# schema-validated Gemini output) — NOT Django REST Framework.
pydantic==2.13.4
email-validator==2.2.0
# Auth.
PyJWT==2.10.1
argon2-cffi==23.1.0
# Gemini (modern unified SDK; sync client for Django's worker model).
google-genai==2.14.0
# Execution provider (sync HTTP) + resume PDF extraction.
httpx==0.28.1
pdfplumber==0.11.4
# Config / logging / DB / CORS / server.
python-dotenv==1.0.1
dj-database-url==2.3.0
psycopg[binary]==3.2.3
django-cors-headers==4.6.0
structlog==24.4.0
gunicorn==23.0.0
